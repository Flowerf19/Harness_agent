# Core dependencies for Discord LLM Chatbot
discord.py
aiohttp
python-dotenv
requests
aiofiles

# Redis for Active Memory storage
redis[hiredis]

# Qdrant for Episodic Memory (Tier 2) storage
qdrant-client

# Testing
pytest
pytest-asyncio
pyyaml

# Vietnamese NLP (still used in handler.py, evernight_adapter.py)
underthesea

# CodeBox for Python code execution (Sandbox)
codeboxapi

# File watching for hot-reload
watchdog